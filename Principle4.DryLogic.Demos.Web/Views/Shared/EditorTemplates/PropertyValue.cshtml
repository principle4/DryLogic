@using Principle4.DryLogic;
@using Principle4.DryLogic.Validation;
@model PropertyValue
@{
	if (Model.ValueType == typeof(Boolean))
	{
		@Html.Partial("EditorTemplates/PropertyValue_Boolean", Model);
	}
	else
	{
		@Html.Partial("EditorTemplates/PropertyValue_Default", Model);
	}
}
