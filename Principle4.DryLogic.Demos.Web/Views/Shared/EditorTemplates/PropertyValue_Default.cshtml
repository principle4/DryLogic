@using Principle4.DryLogic;
@using Principle4.DryLogic.Web.MVC;

@model PropertyValue
@{
  var valAttribs = MVCUtil.GetValidationAttributes(Model);
}
<div class="form-group">
  @Html.Label("", Model.Definition.HumanName, new { @class = "control-label col-md-2" })
    <div class="col-md-10">
      @Html.TextBox("", Model.FormattedValue, valAttribs)
      @Html.ValidationMessage("")
    </div>
</div>